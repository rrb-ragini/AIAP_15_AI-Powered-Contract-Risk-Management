# ─── LLM API Keys ─────────────────────────────────────────────────────────────
# Add these as Railway Environment Variables (never commit real keys to git)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=...

# ─── Backend (Railway: set in backend service) ─────────────────────────────────
# CORS: set this to your Railway frontend URL (e.g. https://my-app.railway.app)
# Leave unset (or "*") for local development
FRONTEND_URL=https://your-frontend.railway.app

# ─── Frontend (Railway: set in frontend service) ───────────────────────────────
# Points the UI at the deployed backend. Falls back to http://localhost:8000 locally.
VITE_API_BASE_URL=https://your-backend.railway.app
